# R3.3C AI Route Provenance and Intent Defect Gate

Date: 2026-08-26

## Status

The provenance and intent defect gate has execution-level evidence for Local, Cloud, deterministic Memory, and the former Unity fallback route. The fallback observation below is historical defect evidence and is not an accepted product path. The Local-LLM-Only response gate supersedes that architecture. Automated and Mac-hosted integration checks pass; the replacement Development iOS build and physical-device failure/recovery gate remain pending. The R3.3C final A-H acceptance remains paused and is not covered by this record.

## Scope

This gate adds Development-only reply provenance, verifies that model routes perform real completions, and closes two intent classification defects. It does not change Character Memory storage, Progress, canonical facts, or action permissions.

## Observed Replies

### Open social chat

For `我今天心情一般般`, the original device run finished as `unity_fallback`, not as an LLM completion. The attempted Cloud route failed and the fixed social reply came from the Unity fallback service.

### Animal friends

Before the fix, `你有什么动物朋友` had `MemoryMentionPolicy=None` but the knowledge retriever incorrectly matched the identity fact through the phrase `什么动物`. The resulting identity answer and citations were a false-positive grounding result. A shared Python/Unity vector now keeps animal-friend questions in `social_chat` with no grounded fact while preserving valid identity and scientific-name questions.

### Explicit recall

Before the fix, `你还记得我做过什么吗` was classified as `None`, while the variant containing `以前` was classified as `ExplicitRecall`. Both variants now resolve through the deterministic Memory path. The observed empty-memory response reported `memory_deterministic`, `memory_recall`, and `explicit_recall` without invoking a Provider.

## Route Evidence

### Local LLM

- Final source: `local_llm`
- Route mode: `local_only`
- Answer mode: `social_chat`
- Fallback: no
- Device elapsed time: 2329 ms
- Adapter elapsed time: 2044 ms
- The Python adapter received the local chat request.
- llama.cpp received an OpenAI-compatible completion request and returned HTTP 200.
- The Qwen2.5-1.5B completion contained 28 generated tokens and completed in approximately 2005 ms.

The Local route is Mac-hosted llama.cpp reached through the Python adapter. It is not embedded inference on the iPhone.

### Cloud LLM

- Final source: `cloud_llm`
- Route mode: `cloud_only`
- Answer mode: `social_chat`
- Fallback: no
- Device elapsed time: 1558 ms
- The Python server recorded an actual Cloud completion with `finalSource=cloud_llm`.

### Former fallback defect

With the Local model route unavailable, the device reported:

- Final source: `unity_fallback`
- Attempt chain: `local_llm > unity_fallback`
- Fallback reason: `local_llm_provider_error`
- Device elapsed time: 96 ms

This confirmed that the final source described the code path that produced the displayed reply. The Local-LLM-Only gate removes this path's user-facing chat authority: the same failure must now produce `system_status` with `local_model_unavailable`.

## Local-LLM-Only Architecture Amendment

The default user path is now Unity -> Python adapter -> Mac-hosted llama.cpp -> Qwen2.5-1.5B. This is local development-machine inference, not inference embedded in the iPhone.

- R2 canonical knowledge is the authority for scientific facts and citations.
- R3.3A current Progress context is the authority for current task state.
- R3.3B Character Memory projection is the authority for past milestones.
- System policy is the authority for chat-history and injection boundaries.
- Qwen is the default user-facing language generator for every successful response.
- Cloud remains an explicit Editor/Development comparison route and is not an automatic fallback.
- Python rule/knowledge helpers and Unity knowledge helpers may retrieve or validate data but cannot complete a user-facing chat response.
- If Qwen is unavailable, the UI displays a system status; it does not save a fake assistant reply to history.

Normal default provenance must report `contentAuthority` independently from `languageGenerator=local_llm` and `finalSource=local_llm`. Failure provenance must report `finalSource=system_status`, `languageGenerator=none`, `fallbackUsed=no`, and a bounded error code.

### Mac-hosted integration evidence

With the Python adapter and Mac-hosted Qwen service running, real `/chat/local` requests produced:

- open social chat: `contentAuthority=none`, `source=local_llm`, no grounding;
- animal-friends chat: `contentAuthority=none`, `source=local_llm`, no identity grounding;
- scientific name: `contentAuthority=canonical_knowledge`, `source=local_llm`, canonical citations retained;
- diet: `contentAuthority=canonical_knowledge`, `source=local_llm`, `groundingTopic=diet`, canonical citations retained;
- explicit Memory recall: `contentAuthority=character_memory`, `source=local_llm`, only the reviewed milestone was stated;
- history boundary including the words `吃什么`: `contentAuthority=system_policy`, `source=local_llm`, no grounding metadata, citation, or action;
- current task: `contentAuthority=current_progress`, `source=local_llm`, bounded task-title language only.

Stopping llama.cpp while leaving the adapter running returned HTTP 503 with `local_model_unavailable`; no Cloud, Python rule reply, server knowledge reply, or Unity chat fallback was produced. Restarting llama.cpp restored `source=local_llm` in the same adapter workflow without changing Progress. These are Mac integration results, not yet the physical-device failure/recovery acceptance.

## Defects Closed

### Coroutine forwarding

Forwarding overloads in `ChatApiClient` yielded a nested enumerator instead of returning it. `CloudLLMProvider` correctly rejected the opaque yield, so the HTTP request never ran. The forwarding overloads now return the real request enumerator, and focused tests traverse the production overload chain.

### Development LAN HTTP policy

Unity Player settings rejected the development device's local HTTP request before transport. The project now permits insecure HTTP only for Development Builds. Non-Development Players remain blocked.

### Safe transport diagnostics

Cloud and Local failures now expose bounded reason codes rather than URLs, response bodies, prompts, or raw exception text. The Development-only panel shows final source, answer mode, route mode, provider attempts, grounding topic, Memory policy/status, fallback state/reason, and elapsed time.

## Isolation

- The provenance UI is compiled only for Editor or Development Builds.
- The panel does not display prompts, user message text, API credentials, Memory JSON, filesystem paths, network addresses, or device identifiers.
- Historical deterministic Memory helpers remain non-authoritative; current Memory and history-boundary user replies must be generated by the explicitly selected LLM from minimized application-owned facts.
- Provenance metadata does not grant grounding, Memory recall, or action authority.

## Automated Verification

- Python: 129/129 passed
- Unity EditMode: 458/458 passed
- Unity PlayMode: 37/37 passed
- C# compile errors: 0
- `NullReferenceException`: 0
- Unhandled exceptions: 0
- Tracked local-path scan: 0 findings
- `git diff --check`: passed

Focused RED/GREEN coverage includes the real Cloud request overload chain, strict transport and routine failure codes, Development-only local HTTP policy, strict provenance values, Recall phrasing, and shared animal-friend retrieval vectors.

## Evidence Handling

Device screenshots and transient server, llama.cpp, Unity, and test logs remain local-only acceptance evidence outside the repository. No credential, signing material, device identifier, local network address, or machine-specific path is committed.

## Remaining Gate

The replacement Development iOS build must still prove Local success, llama.cpp outage as `system_status`, and same-session recovery on the physical device. After that gate, the R3.3C final A-H device scenarios must be resumed and completed separately. This document does not mark R3.3C as accepted.
